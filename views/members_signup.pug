extends layout

block content
    h1 Wanna see the authors of your favorite messages?? Upgrare your account status!
    form(action="", method="post")
        label(for="memberscode") Enter membership code here
        input#membersCode(type="password", name="memberscode")
        input(type="hidden", name="username" value=currentUser.username)
        button(type="submit") Submit
    if errors
        div(class='error-container')
            h2 Please note the following issues with your request
            ul(class="error-list")
                for error in errors
                    li= error.msg